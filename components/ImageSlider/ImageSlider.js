import React from "react";

const slides = [];

export default function ImageSlider({ children: works }) {
  return;
  <div>
    <div>
      console.log(works.image);
      {works.images}
    </div>
  </div>;
}
