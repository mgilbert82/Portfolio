import React from "react";

const slides = [];

export default function Slider({ children: works }) {
  return;
  <div>
    <div>
      console.log(works.image);
      {works.images}
    </div>
  </div>;
}
